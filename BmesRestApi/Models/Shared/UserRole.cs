using System;
namespace BmesRestApi.Models.Shared
{
	public enum UserRole
	{
        Unknown = 0,
        Administrator = 1,
        RegisteredUser = 2
    }
}

// This is where we define the User Roles: